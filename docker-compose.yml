version: '3.8'

services:
  backend:
    build: ./backend/src/ProducManager.Api
    ports:
      - "5000:80"
    container_name: product-api

  frontend:
    build: ./frontend/product-manager-ui
    ports:
      - "4200:80"
    container_name: product-frontend
    depends_on:
      - backend